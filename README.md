# delulu_is_solulu

- **Native iOS (SwiftUI):** [`Ascend/`](Ascend/README.md) — open `Ascend.xcodeproj` in Xcode on macOS.
- **Cross-platform (Expo / React Native):** [`AscendExpo/`](AscendExpo/README.md) — run on Windows with `npx expo start` (Expo Go on device, or web / Android emulator).